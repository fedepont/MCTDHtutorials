      subroutine nocl1sch(y1,y2,y3,v)
      implicit real*8 (a-h,o-z)
      write(6,'(a,/a)') '###########################################',
     +          'nocl1sch is not linked. Run compile with -i option.'
      write(2,'(a,/a)') '###########################################',
     +          'nocl1sch is not linked. Run compile with -i option.'
      stop
      end

      subroutine innocl1sch
      implicit real*8 (a-h,o-z)
      write(6,'(a,/a)') '###########################################',
     +          'nocl1sch is not linked. Run compile with -i option.'
      write(2,'(a,/a)') '###########################################',
     +          'nocl1sch is not linked. Run compile with -i option.'
      stop
      end













