      subroutine hcn(v,y1,y2,y3)
      implicit real*8 (a-h,o-z)
      write(6,'(a,/a)') '###########################################',
     +          'hcn is not linked. Run compile with -i option.'
      write(2,'(a,/a)') '###########################################',
     +          'hcn is not linked. Run compile with -i option.'
      stop
      end

      subroutine hcn1(v,y1,y2,y3)
      implicit real*8 (a-h,o-z)
      write(6,'(a,/a)') '###########################################',
     +          'hcn is not linked. Run compile with -i option.'
      write(2,'(a,/a)') '###########################################',
     +          'hcn is not linked. Run compile with -i option.'
      stop
      end

      subroutine inhcn
      implicit real*8 (a-h,o-z)
      write(6,'(a,/a)') '###########################################',
     +          'hcn is not linked. Run compile with -i option.'
      write(2,'(a,/a)') '###########################################',
     +          'hcn is not linked. Run compile with -i option.'
      stop
      end













