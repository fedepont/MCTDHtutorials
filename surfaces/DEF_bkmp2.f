      subroutine bkmp2(r1,r2,r3,Vtot,dVtot,id)
      real*8     r1,r2,r3,Vtot,dVtot(3)
      integer    id
      write(6,'(a,/a)') '###########################################',
     +          'bkmp2 is not linked. Run compile with -i option.'
      write(2,'(a,/a)') '###########################################',
     +          'bkmp2 is not linked. Run compile with -i option.'
      stop
      end












