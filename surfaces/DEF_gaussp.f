      subroutine gaussp(x1,x2,v)
      implicit none
      real*8  x1,x2,v
      write(6,'(a,/a)') '###########################################',
     +          'gaussp is not linked. Run compile with -i option.'
      write(2,'(a,/a)') '###########################################',
     +          'gaussp is not linked. Run compile with -i option.'
      stop
      end

