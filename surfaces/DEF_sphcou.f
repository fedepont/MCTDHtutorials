      subroutine sphcou(r1,r2,v)
      implicit none
      real*8  r1,r2,v
      write(6,'(a,/a)') '###########################################',
     +          'sphcou is not linked. Run compile with -i option.'
      write(2,'(a,/a)') '###########################################',
     +          'sphcou is not linked. Run compile with -i option.'
      stop
      end

