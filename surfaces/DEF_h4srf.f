      SUBROUTINE h4srf(RAB,RAC,RBC,RAD,RBD,RCD,v)
      IMPLICIT REAL*8 (A-H,O-Z)
      write(6,'(a,/a)') '###########################################',
     +          'h4srf is not linked. Run compile with -i option.'
      write(2,'(a,/a)') '###########################################',
     +          'h4srf is not linked. Run compile with -i option.'
      stop
      end
