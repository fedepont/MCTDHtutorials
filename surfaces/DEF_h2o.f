      subroutine pjt2(r1,r2,theta,v)
      real*8 v,r1,r2,theta
      write(6,'(a,/a)') '###########################################',
     +  'pjt2  needed. Run compile with "-i h2o" option.'
      write(2,'(a,/a)') '###########################################',
     +  'pjt2  needed. Run compile with "-i h2o" option.'
      stop
      end
 
      subroutine surfh2o(r1,r2,r3,v)
      real*8 v,r1,r2,r3
      write(6,'(a,/a)') '###########################################',
     +  'surfh2o  needed. Run compile with "-i h2o" option.'
      write(2,'(a,/a)') '###########################################',
     +  'surfh2o  needed. Run compile with "-i h2o" option.'
      stop
      end



