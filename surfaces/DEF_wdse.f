      subroutine wdse(r1,r2,r3,r4,r5,r6,v)
      real*8     r1,r2,r3,r4,r5,r6,v
      write(6,'(a,/a)') '###########################################',
     +          'wdse is not linked. Run compile with -i option.'
      write(2,'(a,/a)') '###########################################',
     +          'wdse is not linked. Run compile with -i option.'
      stop
      end

