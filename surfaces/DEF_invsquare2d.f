      subroutine invsquare2d(x1,y1,x2,y2,v)
      implicit none
      real*8  x1,y1,x2,y2,v
      write(6,'(a,/a)') '###########################################',
     +          'invsquare is not linked. Run compile with -i option.'
      write(2,'(a,/a)') '###########################################',
     +          'invsquare is not linked. Run compile with -i option.'
      stop
      end

