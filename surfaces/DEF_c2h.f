      subroutine c2hasec(ider,r,hemat,drv)
      implicit real*8 (a-h,o-z)
      real*8  r(*), drv(*)
      write(6,'(a,/a)') '###########################################',
     +          'c2h is not linked. Run compile with -i option.'
      write(2,'(a,/a)') '###########################################',
     +          'c2h is not linked. Run compile with -i option.'
      stop
      end

      subroutine cv2ehf(a,n,d,rm,gam,i,x,v,der)
      implicit real*8 (a-h,o-z)
      real*8  a(*)
      write(6,'(a,/a)') '###########################################',
     +          'c2h is not linked. Run compile with -i option.'
      write(2,'(a,/a)') '###########################################',
     +          'c2h is not linked. Run compile with -i option.'
      stop
      end

      subroutine cv2dc(a,b,c,ider,r,v2,dv2)
      implicit real*8 (a-h,o-z)
      real*8  a(*), b(*), c(*)
      write(6,'(a,/a)') '###########################################',
     +          'c2h is not linked. Run compile with -i option.'
      write(2,'(a,/a)') '###########################################',
     +          'c2h is not linked. Run compile with -i option.'
      stop
      end

      subroutine c2ha1(ider,r,hemat,drv)
      implicit real*8 (a-h,o-z)
      real*8  r(*), drv(*)
      write(6,'(a,/a)') '###########################################',
     +          'c2h is not linked. Run compile with -i option.'
      write(2,'(a,/a)') '###########################################',
     +          'c2h is not linked. Run compile with -i option.'
      stop
      end














